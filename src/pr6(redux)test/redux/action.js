import { createAction } from "@reduxjs/toolkit";

// 8. Створюємо action statusSelect
export const statusSelect = createAction("task/statusSelect")
export const actionContacts = createAction("task/contacts");